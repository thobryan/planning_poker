# AGENTS

- Run all non-git shell commands inside the container using `docker compose exec web`.
- Run git commands on the host machine in the repo root.
- Project root inside the container is `/app`.
