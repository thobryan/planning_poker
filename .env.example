DJANGO_SECRET_KEY=replace-me
# Toggle to True when running locally. Leave False in production.
DJANGO_DEBUG=False
# Allow both localhost and production domains by default; trim as needed.
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1,poker.abrace.eu
DJANGO_CSRF_TRUSTED_ORIGINS=http://localhost,http://127.0.0.1,https://poker.abrace.eu
# Keep cookies secure unless you explicitly need http://localhost.
DJANGO_SECURE_COOKIES=True

# Cloudflare Turnstile (site + secret keys)
TURNSTILE_ENABLED=True
TURNSTILE_SITE_KEY=0x4AAAAAACANVSuOYBMt9Nxe
TURNSTILE_SECRET_KEY=0x4AAAAAACANVSD7Wz-UG07ZGCP4fPvk4D4

ORG_ALLOWED_EMAIL_DOMAIN=welltech.com
ORG_ACCESS_TOKEN_TTL_SECONDS=600

POSTGRES_DB=poker
POSTGRES_USER=poker
POSTGRES_PASSWORD=replace-me

# When running without docker-compose set DB_HOST to your database endpoint.
# docker-compose injects DB_HOST/REDIS_URL automatically.
DB_NAME=poker
DB_USER=poker
DB_PASSWORD=replace-me
DB_HOST=localhost
DB_PORT=5432

REDIS_URL=redis://localhost:6379/0

GUNICORN_WORKERS=4

# Default Caddy host for local dev; set to poker.abrace.eu (or your domain) in prod.
CADDY_DOMAIN=localhost
# Use "internal" for local/self-signed certs or specify an ACME email for public TLS.
CADDY_TLS=internal
# Use high ports locally so Docker doesn’t need privileged access (switch back to 80/443 in prod).
CADDY_HTTP_PORT=8080
CADDY_HTTPS_PORT=8443

# Email configuration for login tokens
# Default backend falls back to console when DJANGO_DEBUG=True; override as needed.
# Uncomment the next line to force console backend even in production-like shells.
# EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=mailhog
EMAIL_PORT=1025
EMAIL_USE_TLS=False
EMAIL_USE_SSL=False
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
DEFAULT_FROM_EMAIL=poker@welltech.com
ERROR_REPORT_EMAIL=poker@abrace.eu
ERROR_EMAIL_MAX_PER_WINDOW=10
ERROR_EMAIL_WINDOW_SECONDS=300

# MailHog UI (dev only)
MAILHOG_SMTP_PORT=1025
MAILHOG_UI_PORT=8025
