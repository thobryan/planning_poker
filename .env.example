DJANGO_SECRET_KEY=replace-me
DJANGO_DEBUG=False
DJANGO_ALLOWED_HOSTS=poker.abrace.eu
DJANGO_CSRF_TRUSTED_ORIGINS=https://poker.abrace.eu
DJANGO_SECURE_COOKIES=True

POSTGRES_DB=poker
POSTGRES_USER=poker
POSTGRES_PASSWORD=replace-me

# When running without docker-compose set DB_HOST to your database endpoint.
# docker-compose injects DB_HOST/REDIS_URL automatically.
DB_NAME=poker
DB_USER=poker
DB_PASSWORD=replace-me
DB_HOST=localhost
DB_PORT=5432

REDIS_URL=redis://localhost:6379/0

GUNICORN_WORKERS=4

CADDY_DOMAIN=poker.abrace.eu
# Use "internal" for a local/self-signed certificate or specify an ACME email.
CADDY_TLS=internal
# Uncomment to override published ports locally (defaults: 80/443)
# CADDY_HTTP_PORT=8080
# CADDY_HTTPS_PORT=8443
